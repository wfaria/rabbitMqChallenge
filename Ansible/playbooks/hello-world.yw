---
- hosts: aspproducers:elkservers:rabbitmqconsumers:rabbitmqservers
  become: yes
  become_method: sudo
  tasks:
  - name: "whoami?"
    shell: whoami
    register: whoami

  - debug: var=whoami.stdout
  
  - debug:
      msg: Hello World!
      
  - debug:
      msg: "Home folder: {{ lookup('env','HOME') }}"